#include <Engine.h>

namespace FM3D {

	//Terrain::Terrain(Texture* heightMap, Vector2f size, Vector2i partCount, Vector2i partSize, Texture* colorMap, Texture* tex0, Texture* tex1, Texture* tex2, Texture* tex3):
	//	m_parts(partCount.x, std::vector<TerrainPart>(partCount.y)),
	//	m_size(size), m_partCount(partCount), m_partSize(partSize),
	//	m_triangleSize((size / Vector2f((float)partCount.x, (float)partCount.y)) / Vector2f((float)partSize.x, (float)partSize.y)) {

	//}

	//TerrainPart::TerrainPart(Vector2i position, std::vector<std::vector<float>> heights): m_position(position), m_heights(heights) {
	//}
}